Feature: Main Menu loads correctly

  Scenario: I can see all the Main Menu's elements
    Then I should see element with id "convertButton"
    Then I should see element with id "selectImageImageView"
    Then I see element with id "showGalleryButton"
    Then I take a picture