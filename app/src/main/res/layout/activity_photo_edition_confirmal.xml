<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_blue"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="projekt_android.photoeditor.PhotoEditionConfirmal">


    <ImageView
        android:id="@+id/editedImageImageView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/shareFacebookButton"
        android:layout_marginBottom="5dp"
        android:adjustViewBounds="true" />

    <Button
        android:id="@+id/saveEditedPhotoButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:onClick="saveEditedPhoto"
        android:text="@string/save_the_photo" />

    <ImageButton
        android:id="@+id/shareFacebookButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/facebooksharebutton"
        android:background="#00000000"
        android:layout_above="@id/saveEditedPhotoButton"
        android:layout_centerHorizontal="true"
        android:onClick="shareOnFacebook"
        />

</RelativeLayout>
